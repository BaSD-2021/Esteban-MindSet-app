import styles from './postulants.module.css';

function Postulants() {
  return (
    <section className={styles.container}>
      <h2>Postulants</h2>
    </section>
  );
}

export default Postulants;
