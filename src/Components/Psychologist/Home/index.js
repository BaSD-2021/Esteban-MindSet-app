import React from 'react';
import styles from './home.module.css';

const Home = () => {
  return (
    <section className={styles.container}>
      <h1>Home</h1>
    </section>
  );
};

export default Home;
